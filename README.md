# 一箭又一箭（One Arrow After Another）

> 软件工程课程第二次个人作业 —— 基于 Python + Pygame 的点击式箭头解谜小游戏

## 游戏简介

"一箭又一箭"是一类点击式箭头解谜游戏。棋盘中分布着若干方向为上 / 下 / 左 / 右的箭头，玩家需要观察箭头之间的阻挡关系，按照正确顺序点击箭头，使所有箭头依次飞出棋盘。

- 若点击的箭头沿指向方向到棋盘边界之间**没有其他箭头**，箭头飞出棋盘并被消除；
- 若路径上存在其他箭头，箭头被挡住，会**左右晃动并变红**，同时消耗 1 次失误、失去 1 颗星；
- 每关限失误 **3 次**、限时 **75 秒**，任一耗尽则本关失败；
- 通关后按失误次数评 **1–3 颗星**（零失误 = 3 星）；
- 共 **7 个关卡**，带选关界面，通关进度自动存档。

## 开发环境

- Python 3.13+
- Pygame 2.x

## 安装与运行

```bash
pip install pygame
python one_arrow_game.py
```

## 操作说明

| 操作 | 说明 |
|---|---|
| 鼠标左键点击箭头 | 射出 / 尝试射出箭头 |
| 重新开始按钮 | 恢复当前关初始布局、时间和失误次数 |
| 选择关卡 | 从主菜单进入选关界面，已解锁关卡显示星级 |
| 返回按钮 | 返回选关界面 |

## 界面

- **主菜单**：开始游戏 / 选择关卡
- **选关界面**：7 个关卡按钮，已通关显示金色星星，未解锁显示锁
- **游戏界面**：当前关卡、剩余箭头、剩余失误、倒计时、重新开始按钮
- **通关界面**：获得星星数 + 剩余时间
- **失败界面**：失败原因（失误用完 / 时间用完）

## 项目结构

```
.
├── one_arrow_game.py   # 游戏主程序
├── test_game.py        # 自动化逻辑测试
├── progress.json       # 通关进度存档（自动生成，已 gitignore）
└── README.md
```

## 实现要点

- **棋盘**：二维列表，单元格为 `None` 或 `'U'/'D'/'L'/'R'`。
- **路径检测**：从被点箭头沿方向向量走到边界，遇非空即阻挡。
- **计时**：每关 75 秒，用 `time.time()` 差值计算。
- **星级**：初始 3 星，每失误一次减 1；通关后存档。
- **存档**：`progress.json` 记录已解锁关卡数和每关最高星数。
- **选关**：顺序解锁，前一关通关后解锁下一关。

## 测试

```bash
python test_game.py
```
